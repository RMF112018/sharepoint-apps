{
  "name": "spfx-embed-apps",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "format": "pnpm -r format"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "prettier": "^3.2.5",
    "typescript": "^5.5.0"
  },
  "volta": {
    "node": "22.14.0",
    "pnpm": "10.13.1"
  }
}

